
import { ArcElement, CategoryScale, Chart as ChartJS, LineController, LineElement, PieController, PointElement, LinearScale, Title, Tooltip } from 'chart.js';

ChartJS.register(ArcElement, CategoryScale, LineController, LineElement, PointElement, PieController, LinearScale, Title, Tooltip);
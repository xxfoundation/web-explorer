import { Typography } from '@mui/material';
import React, { FC } from 'react';

const AuthoredBlocksTable: FC = () => {
  return <Typography>authored blocks</Typography>;
};

export default AuthoredBlocksTable;

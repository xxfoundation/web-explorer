fragment Block on static_Blocks {
  block_hash
  block_number
  block_number_finalized
  current_era
  num_transfers
  total_events
  block_author
  block_author_name
  current_index
  era_length
  era_progress
  extrinsics_root
  is_election
  is_epoch
  new_accounts
  parent_hash
  session_length
  session_per_era
  session_progress
  spec_name
  spec_version
  state_root
  timestamp
  total_issuance
  validator_count
}
